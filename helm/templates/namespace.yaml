{{- if .Values.namespace_create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  labels:
    name: {{ .Values.namespace }}
    {{- include "fishing-map-infrastructure.labels" . | nindent 4 }}
{{- end }}

